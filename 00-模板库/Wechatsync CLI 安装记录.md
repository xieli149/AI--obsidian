# Wechatsync CLI 安装记录

## 安装时间

2026-06-08

## 安装命令

```bash
npm install -g @wechatsync/cli
```

## 验证命令

```bash
wechatsync --version
```

## 验证结果

```text
1.0.0
```

## 下一步

需要在 Chrome 扩展里开启 MCP 连接，并将 Token 写入 `WECHATSYNC_TOKEN` 环境变量，之后才能用 CLI 检查平台状态和同步草稿。
